services:
  app:
    image: 'jc21/nginx-proxy-manager:v3'
    container_name: nginx
    restart: unless-stopped
    ports:
      # These ports are in format <host-port>:<container-port>
      - '80:80' # Public HTTP Port
      - '443:443' # Public HTTPS Port
      - '81:81' # Admin Web Port
      # Add any other Stream port you want to expose
      # - '21:21' # FTP

    # Uncomment the next line if you uncomment anything in the section
    environment:
      # Uncomment this if you want to change the location of
      # the SQLite DB file within the container
      # DB_SQLITE_FILE: "/data/database.sqlite"

      # Uncomment this if IPv6 is not enabled on your host
      DISABLE_IPV6: 'true'

    volumes:
      #- /home/antidote/nginx/data:/data:rw
      #- /home/antidote/nginx/letsencrypt:/etc/letsencrypt:rw
      - "/mnt/Partage/Logiciels/nginx/letsencrypt:/etc/letsencrypt:rw"
      - "/mnt/Partage/Logiciels/nginx/data:/data:rw"
      # chown: changing ownership of '/data/nginx/default_host': Operation not permitted
